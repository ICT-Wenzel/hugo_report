## Deployment auf Railway

1. Repository auf Railway importieren.
2. Environment-Variablen (`SUPABASE_URL`, `PUBLIC_SUPABASE_URL`, `SUPABASE_KEY`) im Railway Dashboard setzen.
3. Deployment starten – Railway erkennt automatisch Streamlit oder nutzt das Procfile.